{
  "enabled": true,
  "name": "Documentation Check",
  "description": "Ensures public APIs are documented",
  "version": "1",
  "when": {
    "type": "fileEdited",
    "patterns": [
      "src/**/*.rs"
    ]
  },
  "then": {
    "type": "askAgent",
    "prompt": "Check the saved Rust file for public APIs (pub fn, pub struct, pub enum):\n\n1. Verify all have doc comments (///)\n2. Check for examples in complex functions\n3. Ensure error cases are documented\n\nIf missing docs, add them immediately. Report: ✅ if all documented, 📝 with additions made."
  }
}