# JESSY MLX Fine-Tuning Requirements (Apple Silicon Only!)

# MLX must be installed from source or via specific command
# See installation instructions in M2_FINETUNING.md

# For now, use PyTorch with MPS (Metal Performance Shaders)
torch>=2.0.0
transformers>=4.35.0
datasets>=2.14.0
peft>=0.6.0
accelerate>=0.24.0
sentencepiece>=0.1.99
