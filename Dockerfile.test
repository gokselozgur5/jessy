# Test runner image with all dependencies
FROM rust:1.85-slim

# Install build and test dependencies
RUN apt-get update && apt-get install -y \
    pkg-config \
    libssl-dev \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# Default command
CMD ["cargo", "test", "--lib"]
