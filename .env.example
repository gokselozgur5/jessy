# JESSY Web Chat Server Configuration

# ============================================
# Required Configuration
# ============================================

# Anthropic API Key (required)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ============================================
# Server Configuration
# ============================================

# Server host (default: 0.0.0.0)
HOST=0.0.0.0

# Server port (default: 8080)
PORT=8080

# ============================================
# Persistent Memory Configuration
# ============================================

# Path to store user contexts (default: ./data/user_contexts)
PERSISTENT_MEMORY_PATH=./data/user_contexts

# Number of days to retain user contexts (default: 30)
MEMORY_RETENTION_DAYS=30

# Maximum number of user contexts to cache in memory (default: 100)
MEMORY_CACHE_SIZE=100

# ============================================
# WebSocket Configuration
# ============================================

# Maximum concurrent WebSocket connections (default: 1000)
WEBSOCKET_MAX_CONNECTIONS=1000

# ============================================
# Feature Flags
# ============================================

# Enable authenticity features (uncertainty, pivots, corrections)
# Options: true, false (default: true)
ENABLE_AUTHENTICITY_FEATURES=true

# ============================================
# Logging Configuration
# ============================================

# Rust log level (default: info)
# Options: error, warn, info, debug, trace
RUST_LOG=info

# Enable backtrace on panic (default: 0)
# Options: 0 (disabled), 1 (enabled), full (full backtrace)
RUST_BACKTRACE=0
